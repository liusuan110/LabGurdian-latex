# LabGuardian-GNN paper figures

This directory now contains both the earlier LaTeX snippets (`*.tex`) and the Typst-native snippets (`*.typ`) used by `../../gnn_paper.typ`. The Typst version is the active one for the GNN final-paper draft.
